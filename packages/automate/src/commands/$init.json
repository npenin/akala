{
    "$schema": "https://raw.githubusercontent.com/npenin/akala/main/packages/commands/command-schema.json",
    "": {
        "inject": [
            "pm",
            "params.0"
        ]
    },
    "cli": {
        "usage": "$init [persist]",
        "inject": [
            "pm",
            "options.persist"
        ],
        "options": {
            "persist": {
                "normalize": true
            }
        }
    },
    "fs": {
        "inject": [
            "pm",
            "params.0"
        ]
    }
}
