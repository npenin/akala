jobs:
  build:
    steps:
      - name: semantic-release
        outputAs: releases
        job: ./semantic-releases.yml
      - name: versions
        dispatch: get-version
        with:
          path: $($.location)
        outputAs: versions
        foreach: $(build.releases.build.bump)
      - name: tag
        foreach: $(build.versions)
        foreach-strategy: wait-for-previous
        run:
          - git
          - tag
          - -a
          - $($index)@$($)
          - -m
          - Publish
      - name: push
        run: git push origin --tags
        with:
          shell: true
      - name: publish
        foreach: $(build.releases.build.bump)
        run: yarn workspace $($.name) npm publish
