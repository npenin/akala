{
    "$schema": "https://raw.githubusercontent.com/npenin/akala/master/packages/commands/command-schema.json",
    "cli": {
        "usage": "new module <name> [destination]",
        "options": {
            "force": {
                "aliases": [
                    "f"
                ]
            },
            "destination": {
                "normalize": true
            }
        },
        "inject": [
            "options.name",
            "context",
            "options.destination"
        ]
    },
    "doc": {
        "description": "create a new module with the given name and destination (if present, cwd otherwise)"
    }
}